# 📚 תיעוד מלא - מערכת פסקי דין רבניים

> **Psak Din Management System** - מערכת חכמה לניהול, חיפוש וקישור פסקי דין למקורות הלכתיים

---

## 📋 תוכן עניינים

1. [סקירה כללית](#סקירה-כללית)
2. [מטרת המערכת](#מטרת-המערכת)
3. [טכנולוגיות](#טכנולוגיות)
4. [מבנה הפרויקט](#מבנה-הפרויקט)
5. [רכיבים עיקריים](#רכיבים-עיקריים)
6. [דפים ותכונות](#דפים-ותכונות)
7. [פונקציות עזר](#פונקציות-עזר)
8. [אינטגרציות](#אינטגרציות)
9. [עיצוב ו-UI](#עיצוב-ו-ui)
10. [התקנה והרצה](#התקנה-והרצה)

---

## 🎯 סקירה כללית

מערכת זו מאפשרת:
- **העלאת פסקי דין** - העלאת קבצי HTML של פסקי דין רבניים
- **חיפוש חכם** - חיפוש בתוכן הפסקים
- **אינדקס מקורות** - זיהוי אוטומטי של מקורות (גמרא, שו"ע, רמב"ם)
- **ניווט בספרות** - גישה לגמרא, שולחן ערוך וספרים דרך API של ספריא
- **קישור דו-כיווני** - מפסק למקור ומהמקור לפסקים

---

## 🎯 מטרת המערכת

### הבעיה:
פסקי דין רבניים מפוזרים, קשה למצוא אותם וקשה לקשר אותם למקורות ההלכתיים.

### הפתרון:
מערכת אחודה שמרכזת את כל הפסקים, מזהה אוטומטית מקורות, ומאפשרת ניווט קל בין פסקים למקורות.

### קהל היעד:
- דיינים
- טוענים רבניים
- עורכי דין
- חוקרי משפט עברי
- כל מי שמתעניין בפסיקה רבנית

---

## 🛠️ טכנולוגיות

### Frontend
| טכנולוגיה | גרסה | תפקיד |
|-----------|-------|--------|
| **React** | 18.3.1 | ספריית UI |
| **TypeScript** | 5.6.2 | שפת תכנות |
| **Vite** | 6.0.1 | כלי בנייה |
| **React Router** | 6.28.0 | ניווט |
| **Tailwind CSS** | 3.4.15 | עיצוב |
| **Lucide React** | 0.460.0 | אייקונים |

### Backend & Services
| טכנולוגיה | תפקיד |
|-----------|--------|
| **Supabase** | מסד נתונים + Auth |
| **Sefaria API** | מקור טקסטים תורניים |
| **localStorage** | שמירה מקומית |

### Hosting & CI/CD
| טכנולוגיה | תפקיד |
|-----------|--------|
| **GitHub** | ניהול קוד |
| **Netlify** | אחסון ופריסה |

---

## 📁 מבנה הפרויקט

```
psak-din-app/
├── 📄 index.html              # קובץ HTML ראשי
├── 📄 package.json            # תלויות npm
├── 📄 vite.config.ts          # הגדרות Vite
├── 📄 tailwind.config.js      # הגדרות Tailwind
├── 📄 tsconfig.json           # הגדרות TypeScript
├── 📄 netlify.toml            # הגדרות Netlify
│
├── 📁 src/
│   ├── 📄 main.tsx            # נקודת כניסה
│   ├── 📄 App.tsx             # רכיב ראשי + Routes
│   ├── 📄 index.css           # סגנונות גלובליים
│   │
│   ├── 📁 components/         # רכיבים משותפים
│   │   └── 📄 Sidebar.tsx     # תפריט ניווט צדדי
│   │
│   ├── 📁 pages/              # דפי האפליקציה
│   │   ├── 📄 Dashboard.tsx
│   │   ├── 📄 PsakimList.tsx
│   │   ├── 📄 PsakView.tsx
│   │   ├── 📄 Favorites.tsx
│   │   ├── 📄 Upload.tsx
│   │   ├── 📄 GemaraExplorer.tsx
│   │   ├── 📄 ShulchanAruchExplorer.tsx
│   │   ├── 📄 SefarimExplorer.tsx
│   │   ├── 📄 SmartIndex.tsx
│   │   ├── 📄 IndexPage.tsx
│   │   ├── 📄 Connections.tsx
│   │   └── 📄 SourceView.tsx
│   │
│   ├── 📁 lib/                # ספריות
│   │   └── 📄 supabase.ts     # חיבור Supabase
│   │
│   └── 📁 utils/              # פונקציות עזר
│       ├── 📄 psakUtils.ts
│       └── 📄 smartReferences.ts
│
├── 📁 supabase/               # הגדרות Supabase
│   └── 📁 migrations/
│
├── 📁 docs/                   # תיעוד
│   ├── 📄 README.md
│   └── 📁 setup/
│
└── 📁 scripts/                # סקריפטים
```

---

## 🧩 רכיבים עיקריים

### 1. Sidebar.tsx - תפריט ניווט

```typescript
// מיקום: src/components/Sidebar.tsx
// תפקיד: תפריט צדדי עם ניווט לכל הדפים

תכונות:
- לוגו וכותרת
- קישורים לכל הדפים
- תמיכה ב-collapse/expand
- עיצוב RTL
```

### 2. App.tsx - רכיב ראשי

```typescript
// מיקום: src/App.tsx
// תפקיד: הגדרת Routes וניהול state גלובלי

Routes:
- / → Dashboard
- /psakim → PsakimList
- /psak/:id → PsakView
- /favorites → Favorites
- /upload → Upload
- /gemara → GemaraExplorer
- /shulchan-aruch → ShulchanAruchExplorer
- /sefarim → SefarimExplorer
- /smart-index → SmartIndex
- /index → IndexPage
- /connections → Connections
- /source/:type/:ref → SourceView
```

---

## 📄 דפים ותכונות

### 1. Dashboard (לוח בקרה)
**קובץ:** `src/pages/Dashboard.tsx`

| תכונה | תיאור |
|--------|--------|
| סטטיסטיקות | מספר פסקים, מקורות, קטגוריות |
| פעולות מהירות | קישורים לפעולות נפוצות |
| פסקים אחרונים | רשימת 5 הפסקים האחרונים |

### 2. PsakimList (רשימת פסקים)
**קובץ:** `src/pages/PsakimList.tsx`

| תכונה | תיאור |
|--------|--------|
| הצגת פסקים | כרטיסיות עם מידע מקוצר |
| חיפוש | חיפוש בכותרת ותוכן |
| סינון | לפי קטגוריה |
| מיון | לפי תאריך, מספר |

### 3. PsakView (צפייה בפסק)
**קובץ:** `src/pages/PsakView.tsx`

| תכונה | תיאור |
|--------|--------|
| תצוגת פסק | HTML מעוצב |
| מידע | מספר, קטגוריה, תאריך |
| מקורות | רשימת מקורות מזוהים |
| מועדפים | הוספה/הסרה ממועדפים |

### 4. Upload (העלאת פסקים)
**קובץ:** `src/pages/Upload.tsx`

| תכונה | תיאור |
|--------|--------|
| העלאת קבצים | גרירה או בחירה |
| תמיכה | קבצי HTML |
| עיבוד | חילוץ מספר, כותרת, תוכן |
| זיהוי מקורות | אוטומטי |

### 5. GemaraExplorer (דפדפן גמרא)
**קובץ:** `src/pages/GemaraExplorer.tsx`

| תכונה | תיאור |
|--------|--------|
| 6 סדרים | זרעים, מועד, נשים, נזיקין, קדשים, טהרות |
| מסכתות | כל המסכתות בכל סדר |
| דפים | ניווט לפי דף ועמוד |
| API ספריא | טעינת טקסט מספריא |

### 6. ShulchanAruchExplorer (דפדפן שו"ע)
**קובץ:** `src/pages/ShulchanAruchExplorer.tsx`

| תכונה | תיאור |
|--------|--------|
| 4 חלקים | או"ח, יו"ד, אה"ע, חו"מ |
| סימנים | ניווט לפי סימן |
| טעינה | מ-API ספריא |

### 7. SefarimExplorer (ספריית ספרים)
**קובץ:** `src/pages/SefarimExplorer.tsx`

| תכונה | תיאור |
|--------|--------|
| קטגוריות | תנ"ך, משנה, רמב"ם, מפרשים, מוסר, קבלה |
| ספרים | כל הספרים בכל קטגוריה |
| פרקים | ניווט לפי פרק |

### 8. SmartIndex (אינדקס חכם)
**קובץ:** `src/pages/SmartIndex.tsx`

| תכונה | תיאור |
|--------|--------|
| עץ היררכי | סדר → מסכת → דף → עמוד |
| זיהוי | מקורות בפסקים |
| קישור | מפסק למקור |
| תצוגה מפוצלת | גמרא + פסק |

### 9. Connections (קישורים)
**קובץ:** `src/pages/Connections.tsx`

| תכונה | תיאור |
|--------|--------|
| מפת קשרים | בין פסקים למקורות |
| ניתוח | מקורות נפוצים |

### 10. SourceView (תצוגת מקור)
**קובץ:** `src/pages/SourceView.tsx`

| תכונה | תיאור |
|--------|--------|
| טקסט מקור | מספריא |
| פסקים קשורים | רשימה לצד הטקסט |
| ניווט | קודם/הבא |

---

## ⚙️ פונקציות עזר

### psakUtils.ts

```typescript
// מיקום: src/utils/psakUtils.ts

// ממשק פסק דין
interface Psak {
  id: string
  number: number
  title: string
  content: string
  htmlContent: string
  category: string
  date: string
  sources: Source[]
  isFavorite: boolean
}

// פונקציות:
parsePsakFromHtml(html: string): Psak
  // מחלץ מידע מקובץ HTML

extractPsakNumber(title: string): number
  // מחלץ מספר פסק מהכותרת

detectCategory(content: string): string
  // מזהה קטגוריה (ממונות, נישואין, גירושין...)

savePsakim(psakim: Psak[]): void
  // שומר ב-localStorage

loadPsakim(): Psak[]
  // טוען מ-localStorage

searchPsakim(query: string, psakim: Psak[]): Psak[]
  // מחפש בפסקים

filterByCategory(category: string, psakim: Psak[]): Psak[]
  // מסנן לפי קטגוריה
```

### smartReferences.ts

```typescript
// מיקום: src/utils/smartReferences.ts

// ממשק מקור
interface Source {
  type: 'gemara' | 'shulchan_aruch' | 'rambam' | 'other'
  reference: string
  seder?: string
  masechet?: string
  daf?: string
  amud?: string
  helek?: string
  siman?: string
  seif?: string
}

// פונקציות:
extractSources(content: string): Source[]
  // מזהה כל המקורות בטקסט

parseGemaraReference(text: string): Source | null
  // מפענח הפניה לגמרא
  // דוגמה: "בבא מציעא דף ל' עמוד א'" → { type: 'gemara', masechet: 'בבא מציעא', daf: '30', amud: 'א' }

parseShulchanAruchReference(text: string): Source | null
  // מפענח הפניה לשו"ע
  // דוגמה: "חו"מ סימן של"ג" → { type: 'shulchan_aruch', helek: 'חושן משפט', siman: '333' }

parseRambamReference(text: string): Source | null
  // מפענח הפניה לרמב"ם

buildSefariaRef(source: Source): string
  // בונה URL לספריא
```

---

## 🔗 אינטגרציות

### Sefaria API

```typescript
// Base URL
const SEFARIA_API = 'https://www.sefaria.org/api'

// Endpoints בשימוש:
GET /texts/{ref}              // קבלת טקסט
GET /index                    // רשימת ספרים
GET /name/{name}              // חיפוש שם

// דוגמה:
fetch('https://www.sefaria.org/api/texts/Bava_Metzia.30a')
  .then(res => res.json())
  .then(data => {
    console.log(data.he)  // טקסט עברי
    console.log(data.text) // תרגום אנגלי
  })
```

### Supabase

```typescript
// מיקום: src/lib/supabase.ts

// חיבור
const supabase = createClient(url, anonKey)

// טבלאות:
- psakim: id, number, title, content, category, created_at

// פעולות:
supabase.from('psakim').select('*')
supabase.from('psakim').insert(psak)
supabase.from('psakim').update(updates).eq('id', id)
supabase.from('psakim').delete().eq('id', id)
```

### localStorage

```typescript
// Keys בשימוש:
'psakim'           // רשימת פסקים
'favorites'        // מועדפים
'sefariaCache'     // מטמון ספריא
'settings'         // הגדרות משתמש
```

---

## 🎨 עיצוב ו-UI

### ערכת צבעים

| צבע | קוד | שימוש |
|-----|-----|-------|
| זהב | `#C9A227` | מסגרות, כפתורים, הדגשות |
| נייבי | `#102a43` | סיידבר, כותרות |
| קרם | `#fdfcfa` | רקעים |
| אפור כהה | `#243B53` | טקסט |

### טיפוגרפיה

| שימוש | גופן |
|-------|------|
| כותרות | Frank Ruhl Libre |
| טקסט | System fonts (עברית) |

### עקרונות עיצוב

- **RTL** - כל האפליקציה בעברית, מימין לשמאל
- **Responsive** - תמיכה במובייל וטאבלט
- **Elegant** - עיצוב יוקרתי ונקי
- **Accessible** - נגישות בסיסית

---

## 🚀 התקנה והרצה

### דרישות
- Node.js 18+
- npm 9+

### התקנה

```bash
# שכפל את הפרויקט
git clone https://github.com/ticnutai/psak-din-app.git
cd psak-din-app

# התקן תלויות
npm install

# צור קובץ .env
cp .env.example .env
# ערוך את .env עם הערכים שלך
```

### הרצה

```bash
# פיתוח
npm run dev

# בנייה
npm run build

# תצוגה מקדימה
npm run preview
```

### פריסה

```bash
# אוטומטית - דחוף ל-GitHub
git push origin main
# Netlify יפרוס אוטומטית

# ידנית
netlify deploy --prod
```

---

## 📊 סטטיסטיקות קוד

| מדד | ערך |
|-----|-----|
| קבצי TypeScript | ~20 |
| רכיבי React | ~15 |
| שורות קוד | ~5,000 |
| דפים | 11 |

---

## 🔮 פיתוחים עתידיים אפשריים

- [ ] חיפוש מתקדם עם פילטרים
- [ ] ייצוא ל-PDF
- [ ] זיהוי מקורות נוספים (טור, ראשונים)
- [ ] השוואת פסקים
- [ ] API ציבורי
- [ ] אפליקציית מובייל

---

## 📝 רישיון

MIT License

---

## 👨‍💻 מפתחים

פותח על ידי JJ121 עם GitHub Copilot

---

*תיעוד זה נוצר: ינואר 2026*
